#include <bits/stdc++.h>
using namespace std;

int solve(string::iterator begin, string::iterator end) {
  auto c = begin;
  if (isdigit(*c)) {
  }
  else if (*c == '[') {
  }
  else 
}

int main() {

}
